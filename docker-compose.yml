# docker-compose.yml
services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=sqlite:///./test.db
      - SECRET_KEY=your_super_secret_key_for_dev_only

  frontend:
    build: ./frontend
    ports:
      - "80:80"
    depends_on:
      - backend